# 🎯 ALUR CONTOH INPUT - English to Python Translator

## 📋 CARA MENGGUNAKAN PANDUAN INI

1. **Jalankan aplikasi:** `python main.py`
2. **Ikuti alur step-by-step** di bawah ini
3. **Masukkan input satu per satu** ke dalam aplikasi
4. **Klik "Translate"** setelah setiap input
5. **Klik "Run Code"** untuk melihat hasil

---

## 🚀 ALUR 1: PENGENALAN DASAR (5 menit)

### **Step 1: Assignment Sederhana**

```
Input: set age to 25
Expected Output: age = 25
```

**Penjelasan:** Membuat variabel `age` dengan nilai 25

### **Step 2: Assignment dengan String**

```
Input: set name to John
Expected Output: name = John
```

**Penjelasan:** Membuat variabel `name` dengan nilai John

### **Step 3: Operasi Aritmatika**

```
Input: add 10 and 5
Expected Output: result = 10 + 5
```

**Penjelasan:** Penjumlahan sederhana

### **Step 4: Conditional Sederhana**

```
Input: if age greater than 18 then print adult
Expected Output: if age > 18:
                     print(adult)
```

**Penjelasan:** Percabangan IF-THEN sederhana

---

## 🎓 ALUR 2: SISTEM PENILAIAN SISWA (10 menit)

### **Step 1: Setup Data Siswa**

```
Input: set student_name to Alice
Expected Output: student_name = Alice
```

### **Step 2: Setup Nilai Matematika**

```
Input: set math_score to 85
Expected Output: math_score = 85
```

### **Step 3: Setup Nilai Bahasa Inggris**

```
Input: set english_score to 92
Expected Output: english_score = 92
```

### **Step 4: Evaluasi Matematika**

```
Input: if math_score greater than 80 then print good_math else print need_improvement
Expected Output: if math_score > 80:
                     print(good_math)
                 else:
                     print(need_improvement)
```

### **Step 5: Evaluasi Bahasa Inggris**

```
Input: if english_score greater than 90 then print excellent_english else print good_english
Expected Output: if english_score > 90:
                     print(excellent_english)
                 else:
                     print(good_english)
```

### **Step 6: Hitung Rata-rata**

```
Input: add math_score and english_score
Expected Output: result = math_score + english_score
```

### **Step 7: Evaluasi Kelulusan**

```
Input: if result greater than 160 then print passed else print failed
Expected Output: if result > 160:
                     print("passed")
                 else:
                     print("failed")
```

**🎯 Hasil Akhir:** Sistem penilaian lengkap dengan evaluasi per mata pelajaran dan kelulusan

---

## 🌡️ ALUR 3: SISTEM MONITORING CUACA (8 menit)

### **Step 1: Setup Suhu**

```
Input: set temperature to 35
Expected Output: temperature = 35
```

### **Step 2: Setup Kelembaban**

```
Input: set humidity to 75
Expected Output: humidity = 75
```

### **Step 3: Setup Kecepatan Angin**

```
Input: set wind_speed to 15
Expected Output: wind_speed = 15
```

### **Step 4: Evaluasi Suhu**

```
Input: if temperature greater than 30 then print hot else print normal
Expected Output: if temperature > 30:
                     print(hot)
                 else:
                     print(normal)
```

### **Step 5: Evaluasi Kelembaban**

```
Input: when humidity greater than 70 do print humid
Expected Output: if humidity > 70:
                     print(humid)
```

### **Step 6: Evaluasi Angin**

```
Input: if wind_speed greater than 20 then print windy else print calm
Expected Output: if wind_speed > 20:
                     print(windy)
                 else:
                     print(calm)
```

### **Step 7: Peringatan Cuaca Ekstrem**

```
Input: if temperature greater than 40 then print extreme_heat
Expected Output: if temperature > 40:
                     print(extreme_heat)
```

**🎯 Hasil Akhir:** Sistem monitoring cuaca dengan berbagai parameter

---

## 🏪 ALUR 4: SISTEM KASIR TOKO (12 menit)

### **Step 1: Setup Harga Barang**

```
Input: set item_price to 50000
Expected Output: item_price = 50000
```

### **Step 2: Setup Jumlah Barang**

```
Input: set quantity to 3
Expected Output: quantity = 3
```

### **Step 3: Setup Status Member**

```
Input: set is_member to true
Expected Output: is_member = true
```

### **Step 4: Hitung Total**

```
Input: multiply item_price by quantity
Expected Output: result = item_price * quantity
```

### **Step 5: Setup Total ke Variabel**

```
Input: set total to 150000
Expected Output: total = 150000
```

### **Step 6: Cek Diskon Member**

```
Input: if is_member equals true then print member_discount else print no_discount
Expected Output: if is_member == true:
                     print(member_discount)
                 else:
                     print(no_discount)
```

### **Step 7: Cek Diskon Pembelian Besar**

```
Input: if total greater than 100000 then print bulk_discount
Expected Output: if total > 100000:
                     print(bulk_discount)
```

### **Step 8: Hitung Pajak**

```
Input: multiply total by 0.1
Expected Output: result = total * 0.1
```

### **Step 9: Evaluasi Pembayaran**

```
Input: if total greater than 200000 then print expensive else print affordable
Expected Output: if total > 200000:
                     print(expensive)
                 else:
                     print(affordable)
```

**🎯 Hasil Akhir:** Sistem kasir lengkap dengan diskon dan pajak

---

## 🎮 ALUR 5: SISTEM GAME SCORING (15 menit)

### **Step 1: Setup Player**

```
Input: set player_name to Gamer123
Expected Output: player_name = Gamer123
```

### **Step 2: Setup Level**

```
Input: set current_level to 5
Expected Output: current_level = 5
```

### **Step 3: Setup Score**

```
Input: set current_score to 2500
Expected Output: current_score = 2500
```

### **Step 4: Setup Lives**

```
Input: set lives_remaining to 3
Expected Output: lives_remaining = 3
```

### **Step 5: Bonus Level**

```
Input: add current_score and 500
Expected Output: result = current_score + 500
```

### **Step 6: Update Score**

```
Input: set current_score to 3000
Expected Output: current_score = 3000
```

### **Step 7: Cek High Score**

```
Input: if current_score greater than 5000 then print high_score else print normal_score
Expected Output: if current_score > 5000:
                     print(high_score)
                 else:
                     print(normal_score)
```

### **Step 8: Cek Level Up**

```
Input: if current_level greater than 10 then print level_up else print continue_playing
Expected Output: if current_level > 10:
                     print(level_up)
                 else:
                     print(continue_playing)
```

### **Step 9: Cek Game Over**

```
Input: if lives_remaining equals 0 then print game_over else print still_alive
Expected Output: if lives_remaining == 0:
                     print(game_over)
                 else:
                     print(still_alive)
```

### **Step 10: Bonus Lives**

```
Input: when current_score greater than 2000 do print bonus_life
Expected Output: if current_score > 2000:
                     print(bonus_life)
```

**🎯 Hasil Akhir:** Sistem game scoring lengkap dengan berbagai kondisi

---

## 📊 RINGKASAN FITUR YANG DIGUNAKAN

### ✅ **Assignment Variabel:**

-   `set variable to value`
-   Mendukung angka, string, boolean

### ✅ **Operasi Aritmatika:**

-   `add A and B`
-   `multiply A by B`
-   `subtract A from B`
-   `divide A by B`

### ✅ **Conditional Statements:**

-   `if condition then action`
-   `if condition then action else other_action`
-   `when condition do action`

### ✅ **Operator Perbandingan:**

-   `greater than` → `>`
-   `less than` → `<`
-   `equals` → `==`

---

## 🎯 TIPS SUKSES

1. **Mulai dari yang sederhana** - Ikuti Alur 1 dulu
2. **Satu input per waktu** - Jangan terburu-buru
3. **Perhatikan format** - Pastikan ada "then" dan "do"
4. **Cek output** - Lihat kode Python yang dihasilkan
5. **Run code** - Test apakah kode berjalan dengan benar

## 🎉 SELAMAT MENCOBA!

Pilih salah satu alur di atas dan ikuti step-by-step. Semua fitur conditional statements sudah diperbaiki dan siap digunakan!
