# 🎯 CONTOH INPUT YANG BISA DIEKSEKUSI - FINAL

## ✅ SEMUA KODE SUDAH DIPERBAIKI DAN BISA DIEKSEKUSI!

### 🚀 **CARA MENGGUNAKAN:**

1. **Jalankan:** `python main.py`
2. **Copy-paste input** dari contoh di bawah
3. **Klik "Translate"** untuk melihat kode Python
4. **Klik "Run Code"** untuk melihat output di Execution Result

---

## 📋 CONTOH 1: SISTEM PENILAIAN SISWA

### **Input Sequence (satu per satu):**

```
1. set student_name to Alice
2. set math_score to 85
3. set english_score to 92
4. if math_score greater than 80 then print good_math else print poor_math
5. if english_score greater than 90 then print excellent_english else print good_english
```

### **Output Python yang Dihasilkan:**

```python
student_name = "Alice"
math_score = 85
english_score = 92
if math_score > 80:
    print("good_math")
else:
    print("poor_math")
if english_score > 90:
    print("excellent_english")
else:
    print("good_english")
```

### **Execution Result:**

```
good_math
excellent_english
```

---

## 🌡️ CONTOH 2: MONITORING CUACA

### **Input Sequence:**

```
1. set temperature to 35
2. set humidity to 75
3. if temperature greater than 30 then print hot else print normal
4. when humidity greater than 70 do print humid
5. if temperature greater than 40 then print danger else print safe
```

### **Output Python:**

```python
temperature = 35
humidity = 75
if temperature > 30:
    print("hot")
else:
    print("normal")
if humidity > 70:
    print("humid")
if temperature > 40:
    print("danger")
else:
    print("safe")
```

### **Execution Result:**

```
hot
humid
safe
```

---

## 🏪 CONTOH 3: SISTEM KASIR

### **Input Sequence:**

```
1. set item_price to 50000
2. set quantity to 3
3. set is_member to true
4. if is_member equals true then print member_discount else print no_discount
5. if item_price greater than 40000 then print expensive_item else print cheap_item
```

### **Output Python:**

```python
item_price = 50000
quantity = 3
is_member = True
if is_member == True:
    print("member_discount")
else:
    print("no_discount")
if item_price > 40000:
    print("expensive_item")
else:
    print("cheap_item")
```

### **Execution Result:**

```
member_discount
expensive_item
```

---

## 🎮 CONTOH 4: GAME SCORING

### **Input Sequence:**

```
1. set player_name to Gamer123
2. set current_score to 2500
3. set lives_remaining to 3
4. if current_score greater than 2000 then print high_score else print low_score
5. when lives_remaining greater than 0 do print still_playing
6. if current_score greater than 5000 then print bonus_level else print normal_level
```

### **Output Python:**

```python
player_name = "Gamer123"
current_score = 2500
lives_remaining = 3
if current_score > 2000:
    print("high_score")
else:
    print("low_score")
if lives_remaining > 0:
    print("still_playing")
if current_score > 5000:
    print("bonus_level")
else:
    print("normal_level")
```

### **Execution Result:**

```
high_score
still_playing
normal_level
```

---

## 🧪 CONTOH 5: QUICK TEST

### **Input Sequence (untuk test cepat):**

```
1. set age to 20
2. set status to active
3. if age greater than 18 then print adult else print minor
4. when status equals active do print running
```

### **Output Python:**

```python
age = 20
status = "active"
if age > 18:
    print("adult")
else:
    print("minor")
if status == "active":
    print("running")
```

### **Execution Result:**

```
adult
running
```

---

## ✨ FITUR YANG SUDAH DIPERBAIKI

### ✅ **Assignment Values:**

-   **String:** `set status to active` → `status = "active"`
-   **Number:** `set age to 25` → `age = 25`
-   **Boolean:** `set is_member to true` → `is_member = True`

### ✅ **Print Statements:**

-   **String Output:** `print adult` → `print("adult")`
-   **Variable Output:** `print age` → `print(age)` (jika age adalah variabel)
-   **Smart Detection:** Otomatis mendeteksi string vs variabel

### ✅ **Conditional Statements:**

-   **IF-THEN:** `if age greater than 18 then print adult`
-   **IF-THEN-ELSE:** `if score greater than 80 then print good else print poor`
-   **WHEN-DO:** `when status equals active do print running`

### ✅ **Execution:**

-   **No NameError:** Semua variabel dan string ditangani dengan benar
-   **Visible Output:** Output muncul di Execution Result
-   **Error-Free:** Kode bisa dijalankan tanpa error

---

## 🎯 TIPS PENGGUNAAN

### **1. Urutan Input:**

-   Selalu set variabel dulu sebelum menggunakannya
-   Ikuti urutan yang diberikan dalam contoh

### **2. Format yang Benar:**

-   `if...then` untuk kondisi sederhana
-   `if...then...else` untuk kondisi dengan alternatif
-   `when...do` untuk pattern alternatif

### **3. Melihat Output:**

-   Setelah klik "Run Code", lihat bagian "Execution Result"
-   Output akan muncul sebagai text yang bisa dibaca

### **4. Debugging:**

-   Jika ada error, periksa apakah variabel sudah di-set
-   Pastikan format conditional menggunakan "then" atau "do"

---

## 🎉 KESIMPULAN

**Semua fitur sudah bekerja dengan sempurna!**

-   ✅ **Kode bisa dieksekusi** tanpa error
-   ✅ **Output terlihat** di Execution Result
-   ✅ **Assignment values** ditangani dengan benar
-   ✅ **Print statements** menghasilkan output yang benar
-   ✅ **Conditional logic** bekerja sesuai ekspektasi

**Selamat mencoba English to Python Translator dengan fitur yang sudah lengkap!**
